# Used by: npm run build:android (vite build --mode android)
# Capacitor WebView is not same-origin as Vercel — point to your deployed site so /api/generate-words works.
VITE_API_ORIGIN=https://words-app-ten.vercel.app
